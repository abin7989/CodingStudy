import java.io.*;
import java.util.*;

public class Main {
	static BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
	static StringBuilder sb = new StringBuilder(3000);
	static StringTokenizer st = null;
	static char map[] = new char[13];;
	static char al[] = new char[13];;
	static int n;
	static int m;

	public static void com(int s, int cnt) {

		if (cnt == m) {
			for (int i = 0; i < m; i++) {
				sb.append(al[i]).append(" ");
			}
			sb.append("\n");
			return;
		}
		for (int i = s + 1; i < n; i++) {
			al[cnt] = map[i];
			com(i, cnt + 1);
		}
	}

	public static void main(String[] args) throws IOException {
		
		st = new StringTokenizer(br.readLine(), " ");

		m = Integer.parseInt(st.nextToken());
		n = Integer.parseInt(st.nextToken());
		String s = br.readLine();
		for (int i = 0,index = 0; i < n; i++,index+=2) {
			map[i] = s.charAt(index);
		}
		com(-1, 0);
		sb.append("\n");
		System.out.println(sb);
	}
}
